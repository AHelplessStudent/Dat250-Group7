package no.group7.restservice.DTO;

// Probably not needed, We don't really need to get a single vote, we could just use a
// set of votes from a poll. As that is the only context we need a vote in I think.
public class VoteDTO {
}
